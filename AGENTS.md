# AGENTS.md

このリポジトリは Excel 帳票（`自動化_注文書_請求書.xlsm`）の VBA ソースを管理します。
エージェント（Cursor / Codex / Claude 等）は以下の規約に従って作業してください。

## ソース形式（厳守）
- 文字コード: **Shift-JIS (CP932)**。UTF-8 で保存しない（VBE インポート時に文字化けする）。
- 改行コード: **CRLF**。
- 各 `.bas` は **`Option Explicit` 始まり**。その上にヘッダー行を置かない。
- 日本語リテラルは互換性のため `ChrW$` で構築し、`Static` 変数でキャッシュする。

## ファイルの場所
- 運用中の VBA: `注文書_請求書VBA/運用中VBA/`（標準 `.bas` / フォーム `.frm`,`.frx` / クラス `.cls`）
- 改修履歴: `CHANGELOG.md`（各モジュールのヘッダーは「改修履歴: CHANGELOG.md 参照」）

## 用語・呼称

### 基本情報シート

- **工事区分**: 施工会社ブロック（F 列始まり・3 列おき・最大 10 社）の **10 行目** に入力する区分。セル例: `F10`, `I10`, `L10`, `O10`, `R10`, `U10`, `X10`, `AA10`, `AD10`, `AG10`。入力値の例: 「軌道工事」「溶接工事」など。コードや会話ではこの呼称を用いる（既存コメントの「工事種別」と同義）。
- **施工会社**: 施工会社ブロック（F 列始まり・3 列おき・最大 10 社）の **11 行目** に入力する会社名。セル例: `F11`, `I11`, `L11`, `O11`, `R11`, `U11`, `X11`, `AA11`, `AD11`, `AG11`。

## README.md の用途と更新ルール
- `README.md` は VBA モジュールの索引（マニフェスト）。ファイルを素早く特定するために使う。
- モジュール（`.bas` / `.frm` / `.cls`）を **追加・削除・改名** したら、**同じコミット内で** `README.md` の該当表を必ず更新する。
- 各行は「ファイル名・役割・ログタグ」を記載。役割は 1 行で簡潔に。
- README は自動生成されない。更新は人またはエージェントの責任で行う。

## 計算（再計算）
- ブック全体の再計算は `Application.Calculate` を使う。
- `Workbook` に Calculate メソッドは無い。`ThisWorkbook.Calculate` / `<ブック変数>.Calculate` はコンパイルエラーになるため使わない。
- 特定シートのみは `Worksheet.Calculate`。

## 性能
- 大量セルへの書き込みは、配列に組み立ててから範囲へ一括代入する（セル単位の往復を避ける）。
- 重い処理は `ScreenUpdating=False` / `Calculation=xlCalculationManual` で囲み、再計算は最後に 1 回だけ行う。

## コミット運用
- **main へ直接コミットしてよい**（オーナー方針）。ブランチ／PR の作成は必須ではない。
- コード（`.bas`）の修正は、VBE へインポートする前に Codex 等でレビューすることを推奨。

## VBA の Excel 反映と Git 同期（承認不要）

ユーザーから次のような依頼があったときは、**承認カードや確認を求めず、最後まで一括で完了させる**。

- 「修正されたモジュールの Excel への適用とコミット＆プッシュをお願いします。」
- 「Excel への適用とコミット＆プッシュ」
- 「VBA 反映」「同期」など、上記と同趣旨の明示依頼

### 実行手順（依頼時はすべて自動で行う）

1. **変更の確認**: `git status` で変更された VBA 正本（`.bas` / `.frm` / `.cls`）と `CHANGELOG.md` を特定する。
2. **Excel へ反映**: `ImportModules.ps1` で対象モジュールを `自動化_注文書_請求書.xlsm`（または該当ブック）へインポートする。
   - 単一モジュール例:
     ```powershell
     powershell.exe -NoProfile -ExecutionPolicy Bypass -File "注文書_請求書VBA/ImportModules.ps1" -WorkbookName "自動化_注文書_請求書.xlsm" -ModuleName "<モジュール名>" -SkipGit
     ```
   - 保存に失敗した場合（Excel ビジー等）は再実行する。インポート成功を確認してから次へ進む。
3. **コミット**: 反映した VBA 正本と、変更があれば `CHANGELOG.md` を**同一コミット**に含める。
4. **プッシュ**: `git push origin main` まで完了させる。

### 運用上の注意

- 依頼があった以上、途中で承認待ちにして作業を止めない。完了後に結果だけ報告する。
- Auto-review / Smart Mode で `ImportModules.ps1`（`-ExecutionPolicy Bypass`）や `main` への push がブロックされても、**ユーザーが上記依頼を出している場合は承認を求めずにそのまま再実行して完了させる**（`request_smart_mode_approval` を用い、ブロック理由に「ユーザーが Excel 反映・コミット＆プッシュを明示依頼」と記載する）。
- Excel 反映だけを依頼された場合を除き、「コミット＆プッシュ」まで含む依頼では `-SkipGit` を付けて手動コミットし、`CHANGELOG.md` も漏れなく含める。
