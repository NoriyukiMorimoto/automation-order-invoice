# 改善提案：パフォーマンス・コード重複・シート監視のフリーズ短縮

対象: `注文書_請求書VBA/運用中VBA/` 全モジュール（標準38 / フォーム / クラス）
目的: (1) シート監視による「応答なし」時間の短縮、(2) 重い処理の軽減、(3) コード重複の集約、(4) 保守性向上。
本書は**分析と提案のみ**です。コード反映・コミットは別途ご確認のうえ実施します。

---

## 優先度サマリ

| # | 分類 | 対象 | 効果 | リスク | 優先度 |
|---|---|---|---|---|---|
| 1 | フリーズ | `Sheet1.Worksheet_Activate` の全更新チェーン | 大（切替毎の待ち解消） | 中 | ★★★ |
| 2 | フリーズ/性能 | `OrderTplRepairAllGeneratedPlaceholderFormulas` の全シート走査 | 大 | 中 | ★★★ |
| 3 | 保守/フリーズ | 画面更新・計算・イベントの save/set/restore 重複（約40箇所） | 中〜大（固まり事故の防止） | 低 | ★★★ |
| 4 | フリーズ | `Workbook_SheetChange` 同期の単価再計算 | 中 | 低 | ★★ |
| 5 | 性能 | 業者別・per-block の書式付与の反復 | 中 | 中 | ★★ |
| 6 | 重複/整合 | 日本語リテラルの二重管理（ChrW$ と生リテラル混在） | 中（保守性） | 低 | ★★ |
| 7 | バグ | `mod_common.bas` の CP932 不正バイト | 中（文字化け/例外） | 低 | ★★ |
| 8 | 保守 | 巨大モジュール・`*.pre_split` / `*.bak` の残置 | 中 | 低 | ★ |
| 9 | 性能（小） | `DebugLog.Log` のイベント先頭での無条件呼び出し | 小 | 低 | ★ |

---

## A. シート監視の「応答なし」短縮

### A-1. 基本情報シートの `Worksheet_Activate` が毎回フル更新（★★★）

`Sheet1.Worksheet_Activate` は**シートを表示するたびに**以下を同期実行しています。

- `RefreshBranchOfficeValidation True`
- `EnsureVendorCountInputValidation`
- `OrderTplRepairAllGeneratedPlaceholderFormulas`（全シート走査：A-2参照）
- `InitVendorBlockCountFromSheet` / `RefreshVendorListForBasicInfo`
- `RefreshUnitPriceProjectNameValidation`
- `RefreshBasicInfoConstructionTotals`
- `InitBasicInfoGuide`

他シートと基本情報を行き来するだけで毎回これらが走るため、切替時の待ちの主因になります。

**提案**
- 「初回アクティブ化のみ実行」＋「データ変更時に立てる dirty フラグで再実行」に分離する。検証やリスト再構築は毎回不要。
- 重い `OrderTplRepairAllGeneratedPlaceholderFormulas` と `RefreshBasicInfoConstructionTotals` は、生成シートや金額の変更があったときだけ実行するようフラグ化。
- 効果: 通常のシート切替はほぼ即応答に。

### A-2. `OrderTplRepairAllGeneratedPlaceholderFormulas` の全シート×全数式走査（★★★）

`mod_OrderTpl_Shared` のこの処理は、**全ワークシートを走査**し、生成シート（支店控/受注者用/注文請書）ごとに `UsedRange.SpecialCells(xlCellTypeFormulas)` を取り、**1セルずつ `cell.MergeArea`（COM往復）** を評価します。業者が多いほど「シート数 × 数式セル数」の往復が発生し、A-1 と F9 変更後・Refresh 後にも都度走ります。

**提案**
- 実行契機を絞る（テンプレート生成/再作成が起きたときだけ）。dirty フラグ管理。
- セル単位の `.MergeArea` をやめ、対象範囲の `.Formula`（2次元配列）を一括取得して判定 → 必要セルのみ書換。往復回数を桁で削減。
- 効果: アクティブ化・F9 変更後の待ちを大幅短縮。

### A-3. `Workbook_SheetChange` の単価再計算が同期実行（★★）

施行指示書/通知書の施工会社列・整理番号列を編集すると、`RefreshSubcontractorPriceColumns` が**デバウンスなしで同期実行**されます（内訳明細への再転記 `ScheduleOrderDetailRefresh` は約1秒デバウンス済み）。同関数は毎回「業者収集」「レイアウト整合チェック」を行うため、1セル編集でも固定オーバーヘッドがあります。

**提案**
- 既存行の値変更だけのときは「レイアウト整合チェック」をスキップ（列構成が変わらないケースの早期判定）。
- 連続入力に備え、単価再計算も内訳明細と同様に短いデバウンス（`OnTime`）へ寄せる案も検討可（整合性とのトレードオフ）。

### A-4. `DebugLog.Log` がイベント先頭で無条件実行（★）

`Sheet1.Worksheet_Change` / `Worksheet_SelectionChange` は、監視対象外判定より前に `Log` を呼び、`GetTargetValueText(target)` など**文字列を毎回構築**します（`Log` 自体はメモリリングバッファで軽量ですが、文字列連結と `Debug.Print` は毎回発生）。

**提案**
- デバッグ有効フラグでガード、または「無視判定（早期Exit）」の後にログを移動。ホットパスの無駄を削減。

---

## B. 重い処理（性能ホットスポット）

### B-1. 画面更新・計算・イベントの save/set/restore 重複（★★★）

`Application.ScreenUpdating/Calculation/EnableEvents` の「退避→変更→復元」ブロックが**約40箇所**に分散（`mod_BasicInfoUpdate` 6、`mod_WeldingUnitPrice` 5、`Sheet1` 4、他多数）。エラー経路で復元漏れがあると、以後 Excel が手動計算・イベント無効のまま残り、**体感フリーズや「反映されない」不具合**の温床になります。

**提案**
- 復元を保証する小さなクラス `PerfGuard`（`Class_Initialize` で退避＋設定、`Class_Terminate` で復元）を新設。各プロシージャ先頭で `Dim g As New PerfGuard` の1行に置換。
- 効果: 重複コードの集約＋「戻し忘れ」起因の固まりを構造的に排除。

### B-2. 業者別・ブロック別の書式付与の反復（★★）

`mod_VendorUnitPrice` / `mod_WeldingUnitPrice` / `mod_Construction_LineMapping` / `mod_OrderTpl_Detail` / `mod_BasicInfoGuide` に `.Interior` / `.Borders` / `.Font` の付与が多数。多くは範囲単位（セル単位ではない）で良好ですが、**業者ループ×リフレッシュ回数**で総量が膨らみます。値の一括代入（配列→範囲）は `mod_OrderTpl_Detail` で実施済みで良い型。

**提案**
- 書式は「テンプレート範囲に一度だけ設定→ `Range.Copy`/`PasteSpecial(xlFormats)` で複製」など、付与回数を削減。
- リフレッシュ自体の発火回数を A 章の dirty 化で減らすと相乗効果。

### B-3. 巨大モジュールの分割継続（★）

`mod_MaterialPriceImport`（約2,879行）、`mod_WeldingUnitPrice`（約2,218行）、`mod_VendorUnitPrice`（約1,928行）、`mod_Construction_LineMapping`（約1,886行）。既に `*.pre_split` の分割実績あり。可読性・コンパイル単位の観点で継続分割を推奨。

---

## C. コード重複

### C-1. 日本語リテラルの二重管理（★★）

同じ語（例「整理番号」）が **ChrW$ ビルダー**（`ThisWorkbook.cls`・`mod_WeldingUnitPrice.bas`）と **生の日本語リテラル**（`mod_Construction_SubconPrice` の `FindHeaderColumn(ws, "整理番号")` 等）で**別々に記述**されています。ヘッダー語・シート名接尾辞・ラベルが各モジュールに散在。

**提案**
- ドメイン用語（ヘッダー名「整理番号/工種分類/施工業者」等、シート名接尾辞、ラベル）を1モジュール（例 `mod_Terms`）に `Static` キャッシュ付きで集約し、各所から参照。
- 生リテラルは CP932 依存を避けるため ChrW$ 版へ寄せる（CLAUDE.md 規約とも整合）。重複と表記ゆれを同時に解消。

### C-2. 状態退避/復元・辞書生成・行末走査の定型化（★）

`CreateObject("Scripting.Dictionary")` の生成や `.End(xlUp/xlToLeft)` による最終行/列取得が各所に点在（`GetLastDataRow` 等の共通化は一部実施済み）。B-1 の `PerfGuard` と併せ、共通ユーティリティへの寄せ集約余地あり。

---

## D. 不具合・保守性

### D-1. `mod_common.bas` の CP932 不正バイト（★★）

整合性検査（`_verify_vba_integrity.py`）で `mod_common.bas` が**CP932 厳格デコード不可（byte 9528 付近）**と検出。該当箇所はコメント（「…ドキュメント…マスタ…」相当）で、半角化け等の不正バイトを含みます。VBE 取込時の文字化けや例外の潜在要因。

**提案**: 当該コメント行を正しい CP932 で再記述。

### D-2. `*.pre_split` / `*.bak` の運用フォルダ残置（★）

`mod_VendorMaster.bas.pre_split` 等が `運用中VBA/` に残り、整合性検査で「pre_split のプロシージャが全モジュールに無い」と3件検出（分割完了済みのため実害はないが**ノイズ**）。`mod_Construction_Order_Import.bas.bak`（約228KB）も残置。

**提案**: 参照専用フォルダ（例 `search.exclude/` 配下）へ退避、または削除。検査ノイズと取込対象の誤りを回避。

---

## 推奨着手順（段階リリース）

1. **B-1 `PerfGuard` 導入**（低リスク・全体波及、フリーズ事故の予防）。
2. **A-2 の一括 `.Formula` 化 ＋ A-1 の dirty フラグ化**（体感効果が最大）。
3. **A-3/A-4 のイベント経路軽量化**。
4. **C-1 用語集約 ＋ D-1/D-2 の整合是正**（重複・検査ノイズの解消）。
5. **B-2/B-3 の書式最適化・分割継続**（中期）。

各項目は独立に適用可能です。着手する項目をご指定いただければ、対象モジュールの詳細設計と差分案（CP932/CRLF 厳守、VBE 取込前レビュー可能な形）を作成します。
